@using TelerikAcademy.ForumSystem.Web.Models.Home
@using Microsoft.AspNet.Identity

@model MealViewModel

<div class="col-md-4">
    <h2>@Model.Title</h2>
    <img src=@Model.Image />
    <p>@Model.Content</p>
    <p>@Model.Price $</p>

    @Html.ActionLink("Order", "Order", "Order", new { userName = User.Identity.Name, meal = Model.Title }, htmlAttributes: new { id = "Order" })
</div>
